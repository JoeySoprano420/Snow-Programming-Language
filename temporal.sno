## Example 3: Durations and Temporal Loops ##

Fn = [main];

let cycles = 5;

Say("Starting temporal loop...");

every 100ms:
 Say("Cycle: ", cycles);
    cycles = cycles - 1;
    
    if cycles == 0:
        Say("Complete!");
        break;
end;

Say("Loop finished");
